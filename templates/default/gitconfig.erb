[filter "lfs"]
        clean = git-lfs clean %f
        smudge = git-lfs smudge %f
        required = true
[user]
        name = <%= @gitname %>
        email = <%= @gitemail %>
[push]
        default = simple
